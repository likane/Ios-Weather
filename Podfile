project 'w3.xcodeproj'

#Uncomment this line to define a global platform for your project
 #platform :ios, '9.0'

target 'w3' do
pod "ForecastIO"
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for w3

  target 'w3Tests' do
  pod "ForecastIO"
    inherit! :search_paths
    
    # Pods for testing
  end

  target 'w3UITests' do
  
  pod "ForecastIO"
    inherit! :search_paths
     #Pods for testing
  end

end
